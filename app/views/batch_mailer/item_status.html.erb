<h2>Hey %recipient.name%,</h2>
<p>
  Just wanted to let you know that <%= link_to @item.title.titleize, list_item_url(@item.list, @item) %>
  <%= @status %>.<br>
  List: <%= @item.list.title.titleize %><br>
  Items Completed: <%= @item.list.percent_complete %>%<br>
  <% if @item.date_completed.present? %>
    Date: <%= @item.date_completed.localtime.strftime("%a %m/%d/%Y %l:%M %p %Z") %>
  <% end %>
</p>
<h3>Pending:</h3>
<ul>
  <% @pending_items.each do |item| %>
    <li><em><%= item.title.titleize %></em></li>
  <% end %>
</ul>
