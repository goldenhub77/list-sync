<h1><%= @list.title %></h1>
<%= link_to "Edit", edit_list_path(@list), class: "btn btn-primary" %>
<%= link_to "Delete", list_path(@list), method: "delete", class: "btn btn-danger", data: { confirm: "Are you sure?" } %>
