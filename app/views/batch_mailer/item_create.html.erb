<h1>Hey, %recipient.name%,</h1>
<p>
  A new item was added to list <%= link_to @item.list.title.titleize, list_url(@item.list) %>
  on <%= @item.created_at %> which you are collaborating on,
  The details are as follows:<br>
  Member: <%= link_to @user.email, user_url(@user) %><br>
  Item: <%= link_to @item.title.titleize, list_item_url(@item.list, @item) %>.<br>
</p>
