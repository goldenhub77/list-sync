Hey, <%= @user.name %>,

You <%= @status %> <%= link_to @item.title.titleize, list_item_url(@item.list, @item) %>
which you are collaborating on.
The details are as follows:
List: <%= link_to @item.list.title.titleize, list_url(@item.list) %>
<% if @item.date_completed.present? %>
  Date: <%= @item.date_completed.localtime.strftime("%a %m/%d/%Y %l:%M %p %Z") %>
<% end %>
