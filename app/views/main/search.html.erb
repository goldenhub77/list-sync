<h1><%= pluralize(@lists.count + @users.count, 'result') %></h1>
<% if @lists.empty? && @users.empty? %>
  <h2>No search results for '<%= session['q'] %>' found.</h2>
<% end %>
<%= render 'shared/search_results', collection: @lists %>
<%= render 'shared/search_results', collection: @users %>
