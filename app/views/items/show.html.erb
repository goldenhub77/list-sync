<h1><%= @item.title %></h1>
<%= link_to "Edit", edit_item_path(@item), class: "btn btn-primary" %>
<%= link_to "Delete", item_path(@item), method: "delete", class: "btn btn-danger", data: { confirm: "Are you sure?" } %>
